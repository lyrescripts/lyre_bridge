# Server Custom Overrides

Drop project-specific server override `.lua` files in this folder when you need runtime custom code.

Shipped examples live in `examples/server` so they are not loaded in production.
