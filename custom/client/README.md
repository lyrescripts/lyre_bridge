# Client Custom Overrides

Drop project-specific client override `.lua` files in this folder when you need runtime custom code.

Shipped examples live in `examples/client` so they are not loaded in production.
