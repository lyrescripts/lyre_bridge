CREATE TABLE lyre_illegalmissions_datas (
    `identifier` VARCHAR(99) PRIMARY KEY,
    `reputation` INT,
    `stat-wins` INT,
    `stat-losses` INT,
    `stat-solo` INT,
    `stat-team` INT,
    `stat-winstreak` INT,
    `stat-maxwinstreak` INT,
    `stat-totalmoney` INT,
    `stat-maxrepuation` INT
);